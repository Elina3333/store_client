import React, {useState} from 'react';
import {
    Alert,
    AlertTitle,
    Button,
    ButtonGroup,
    Container,
    List,
    ListItem,
    ListItemText,
    Typography
} from "@mui/material";
import agent from "../../../api/agent";

const AboutPage = () => {

    return (
       <h2>
           About page
       </h2>
    );
};

export default AboutPage;